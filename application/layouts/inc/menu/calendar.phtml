<?php
	foreach($this->data['data'] as $submenu){
		$preTag = '<a href="/calendar/detail/'.$submenu['calendar_id'].'/">';
		$postTag = '</a>';
		if ($submenu['calendar_id'] == $this->selectedId){
			$preTag = '';
			$postTag = '';
		}
?>
		<p><?php echo $preTag.$submenu['calendar_title'].$postTag;?></p>
<?php
	}
?>